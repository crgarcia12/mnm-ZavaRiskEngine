# .NET Upgrade Assessment

The `dotnet-upgrade-assessment` skill requires MCP tools `get_state`, `get_scenarios`, `initialize_scenario`, and `generate_dotnet_upgrade_assessment`. These tools are not available in this execution environment, so the upgrade assessment could not be executed.
